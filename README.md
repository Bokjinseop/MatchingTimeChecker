# MatchingTimeChecker
템플릿 이미지와 매칭되는 지점의 동영상 시간을 체크하는 프로그램

# Installation
## OpenCV, Pandas, NumPy 설치
    ``` bash
    pip install opencv-python pandas numpy
    ```

# Run
    ```
    python ./detector.py
    ```